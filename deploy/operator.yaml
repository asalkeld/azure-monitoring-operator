apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-monitoring-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: azure-monitoring-operator
  template:
    metadata:
      labels:
        name: azure-monitoring-operator
    spec:
      serviceAccountName: azure-monitoring-operator
      containers:
        - name: azure-monitoring-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - azure-monitoring-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "azure-monitoring-operator"
